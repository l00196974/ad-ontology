# ⚠️ crawl4ai 声明依赖 lxml~=5.3，scrapling 依赖 lxml>=6.0.2
# 实测 crawl4ai 0.8.6 + lxml 6.0.2 可正常运行，需分步安装：
#   1. pip install scrapling>=0.4.2              （先装 scrapling，拉入 lxml 6）
#   2. pip install crawl4ai==0.8.6 --no-deps     （跳过依赖检查）
#   3. pip install -r requirements.txt            （补齐 crawl4ai 其余依赖）
#
# 或直接运行一键安装脚本：bash install.sh

# --- scrapling + 本项目直接依赖 ---
scrapling>=0.4.2          # Scrapling 多引擎爬虫（依赖 lxml>=6.0.2）
curl_cffi>=0.7.0          # scrapling AsyncFetcher 运行时依赖（未声明）
browserforge>=1.2.0       # scrapling 浏览器指纹生成（未声明）
msgspec>=0.19.0           # scrapling 快速序列化（未声明）
markdownify>=0.14.1       # HTML → Markdown 转换（Scrapling 引擎使用）
PyYAML>=6.0               # YAML 配置文件解析

# --- crawl4ai 的全部依赖（除 lxml，由 scrapling 提供） ---
aiofiles>=24.1.0
aiohttp>=3.11.11
aiosqlite~=0.20
anyio>=4.0.0
unclecode-litellm==1.81.13
numpy<3,>=1.26.0
pillow>=10.4
playwright>=1.49.0
patchright>=1.49.0
python-dotenv~=1.0
requests~=2.26
beautifulsoup4~=4.12
playwright-stealth>=2.0.0
xxhash~=3.4
rank-bm25~=0.2
snowballstemmer~=2.2
pydantic>=2.10
pyOpenSSL>=25.3.0
psutil>=6.1.1
nltk>=3.9.1
rich>=13.9.4
cssselect>=1.2.0
httpx>=0.27.2
fake-useragent>=2.0.3
click>=8.1.7
chardet>=5.2.0
brotli>=1.1.0
humanize>=4.10.0
lark>=1.2.2
alphashape>=1.3.1
shapely>=2.0.0
